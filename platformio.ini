[common]
# espressif32 7.x ships arduino-esp32 3.x (ledcAttach, dma_desc_num, etc.)
platform = https://github.com/pioarduino/platform-espressif32.git#54.03.20
board = lolin_d32_pro
framework = arduino
monitor_speed = 115200
board_build.f_cpu = 240000000L
# Dual-6MB-slot OTA layout for the real 16 MB chip (board def claims 4 MB).
# Switching tables requires one flash over USB; afterwards OTA via web UI.
board_build.partitions = partitions_16mb_ota.csv
board_upload.flash_size = 16MB
board_upload.maximum_size = 6291456
build_flags =
    -DBOARD_HAS_PSRAM
    -mfix-esp32-psram-cache-issue
    -DA2DP_LEGACY_I2S_SUPPORT=1
lib_deps =
    https://github.com/moononournation/Arduino_GFX.git
    bblanchon/ArduinoJson@^7
    https://github.com/pschatzmann/ESP32-A2DP.git

[env:release]
extends = common

# 4 MB D32 Pro variant (GD25LQ32 flash chip) — same code, smaller OTA slots.
# Current firmware (~1.9 MB) leaves ~110 KB headroom per slot; if it ever
# outgrows the slot, web OTA fails cleanly and the board needs USB + a
# table rethink.
[env:release-4mb]
extends = common
board_build.partitions = partitions_4mb_ota.csv
board_upload.flash_size = 4MB
board_upload.maximum_size = 2031616

[env:debug]
extends = common
build_flags =
    ${common.build_flags}
    -DDEV_MODE

# --- Native host tests (pure logic only) ---
# Run with: ~/.platformio/penv/bin/pio test -e native
[env:native]
platform = native
test_framework = unity
build_flags =
    -std=gnu++17
    -DUNIT_TEST
    -I src
lib_deps =
    bblanchon/ArduinoJson@^7
lib_compat_mode = off
test_build_src = no
